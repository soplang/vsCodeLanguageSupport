// Test: Type Checking
// This test checks if the nuuc function works properly for detecting types

bandhig("Testing type checking (nuuc):")

// Create variables of different types
tiro number_var = 42
qoraal string_var = "Soplang waa luqad wacan"
labadaran bool_var = been
liis list_var = [1, 2, 3, "afar", been]
shey object_var = {
    magac: "Ahmed",
    da: 25
}
door dynamic_var = "dynamic"

// Check types with nuuc function
bandhig("  Type of number_var: " + nuuc(number_var))
bandhig("  Type of string_var: " + nuuc(string_var))
bandhig("  Type of bool_var: " + nuuc(bool_var))
bandhig("  Type of list_var: " + nuuc(list_var))
bandhig("  Type of object_var: " + nuuc(object_var))
bandhig("  Type of dynamic_var: " + nuuc(dynamic_var))

// Change type of dynamic variable and check again
dynamic_var = 100
bandhig("  Type of dynamic_var after change: " + nuuc(dynamic_var))

// Check type of literal values
bandhig("  Type of literal number: " + nuuc(123))
bandhig("  Type of literal string: " + nuuc("test"))
bandhig("  Type of literal boolean: " + nuuc(run))
bandhig("  Type of literal list: " + nuuc([1, 2, 3]))
bandhig("  Type of literal object: " + nuuc({a: 1, b: 2}))
