// Test: For Loops
// This test checks if for loops work properly

qor("Testing for loops (ku_celi):")

// Simple for loop
qor("  Simple for loop:")
ku_celi i min 1 ilaa 5 {
    qor("    Iteration: " + i)
}

// For loop with step
qor("  For loop with custom step (every other number):")
ku_celi j min 2 ilaa 10 by 2 {
    qor("    Iteration: " + j)
}

// For loop with break
qor("  For loop with break (jooji):")
ku_celi i min 1 ilaa 10 {
    haddii (i > 5) {
        jooji
    }
    qor("    Iteration: " + i)
}

// For loop with continue
qor("  For loop with continue (sii_wad):")
ku_celi i min 1 ilaa 5 {
    haddii (i % 2 == 0) {
        sii_wad
    }
    qor("    Odd number: " + i)
}

// Nested for loops
qor("  Nested for loops:")
ku_celi i min 1 ilaa 3 {
    qor("    Outer loop: " + i)
    ku_celi j min 1 ilaa 2 {
        qor("      Inner loop: " + j)
    }
}

// For loop operating on a list
liis numbers = [10, 20, 30, 40, 50]
qor("  For loop iterating over a list with indices:")
ku_celi i min 0 ilaa numbers.length() - 1 {
    qor("    Element at index " + i + ": " + numbers[i])
}
