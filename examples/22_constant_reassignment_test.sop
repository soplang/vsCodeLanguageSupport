// Test: Constant Reassignment Error
// This test verifies that constants cannot be reassigned

qor("Testing constant reassignment protection:")

// Define some constants
madoor PI = 3.14159
madoor abn MAX_VALUE = 100

qor("  Initial values:")
qor("  PI = " + PI)
qor("  MAX_VALUE = " + MAX_VALUE)

// Trying to reassign a constant
qor("Attempting to reassign PI (should cause error):")
PI = 3.14 // This should fail with a runtime error

// We should never reach this point
qor("  This line should never be displayed")
