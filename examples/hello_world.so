// Simple Soplang Hello World program with .so extension
// This will test our run functionality with .so files

// Print a message to the console
qor("Hello from .so file!");

// Define a simple function
howl multiply(a, b) {
    soo_celi a * b;
}

// Use the function and print the result
door result = multiply(6, 7);
qor("6 * 7 = " + result);

// Simple conditional
haddii (result > 40) {
    qor("Result is greater than 40");
} haddii_kalena {
    qor("Result is less than or equal to 40");
}

qor("Test program with .so extension completed successfully!");